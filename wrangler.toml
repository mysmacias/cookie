name = "cookie"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

[[d1_databases]]
binding = "DB"
database_name = "cookie-db"
database_id = "bfb956b9-aaf1-44d3-ab98-bbcf59ca90b0"
migrations_dir = "migrations"

# Enable R2 in the Cloudflare dashboard, create the bucket, then uncomment:
# [[r2_buckets]]
# binding = "MEDIA_BUCKET"
# bucket_name = "cookie-media"

# Secrets (set per environment, not committed):
#   npx wrangler pages secret put ANTHROPIC_API_KEY
# Used by functions/api/scan-recipe.ts for the photo-scan feature.
