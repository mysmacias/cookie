{
  "name": "cookie-recipe-intelligence",
  "version": "1.0.0",
  "description": "On-device recipe scan via Vision OCR and Apple Foundation Models (iOS 26+).",
  "main": "dist/esm/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "files": [
    "dist/",
    "ios/Sources",
    "Package.swift"
  ],
  "keywords": ["capacitor", "plugin", "native"],
  "license": "MIT",
  "peerDependencies": {
    "@capacitor/core": ">=8.0.0"
  },
  "devDependencies": {
    "@capacitor/core": "^8.3.0",
    "typescript": "~5.8.2"
  },
  "capacitor": {
    "ios": {
      "src": "ios"
    }
  },
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist"
  }
}
